-- Setting up virtual env
-- ACtivating and Installing dependencies inside virtual env

$ cd .
$ python3 -m venv venv_pd
$ . ./venv_pd/bin/activate
$ (venv_pd) pip install -r requirements.txt

-- To deactivate:
$ deactivate

-- Start jupyter notebook
$ jupyter notebook
